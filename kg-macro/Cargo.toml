[package]
name = "kg-macro"
version = "0.1.0"
edition = "2024"
description = "Proc macros for kg mapping docs"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
quote = "1"
syn = { version = "2", features = ["full"] }
