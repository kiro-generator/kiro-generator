[agents]
base = { template = true }
empty = {  }
dependabot = { inherits = ["aws-test"] }
iam = { template = true }
eks = { template = true }
aws-secops = { inherits = ["base", "iam", "eks"] }

[agents.aws-test]
inherits = ["base"]

[agents.aws-test.hooks.agentSpawn.whoami]
command = "aws get caller-identity"
